name: Update Data

on:
  workflow_dispatch:
  schedule:
    - cron: "50 18 * * *"

permissions:
  contents: write

jobs:
  update-data:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install google-cloud-bigquery google-cloud-secret-manager openpyxl

      - name: Configure Google credentials
        env:
          GCP_SA_KEY: ${{ secrets.GCP_SA_KEY }}
        run: |
          test -n "$GCP_SA_KEY"
          printf '%s' "$GCP_SA_KEY" > "$RUNNER_TEMP/gcp-sa.json"
          echo "GOOGLE_APPLICATION_CREDENTIALS=$RUNNER_TEMP/gcp-sa.json" >> "$GITHUB_ENV"

      - name: Rebuild archived observations
        run: python build_observed_archive_supplement.py

      - name: Refresh baseline channel status
        run: python refresh_baseline_channel_status.py

      - name: Refresh former competitor statistics
        run: python refresh_youtube_current_stats.py

      - name: Generate data from BigQuery
        run: python generate_portal_data_bq.py

      - name: Validate public scope and asset links
        run: python validate_generated_scope.py

      - name: Commit updated data
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data reports data_sources/observed_archive_supplement.csv data_sources/youtube_channel_status.csv data_sources/youtube_current_stats.csv
          if git diff --cached --quiet; then
            echo "No data changes."
          else
            git commit -m "Update data from BigQuery"
            git push
          fi
